This is a basic Retrieval Augmented Generation system. It allows for the LLM of choice to access documents with in the system, currently is only for a document within the folder
but it can be converted to any document in the PC system it requires the correct path for the file.
